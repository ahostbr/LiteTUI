# T708: Creator Magic workflow versus our Resolve CLI

2026-09-12 — RustAxis — source inspection only. No Resolve process, CLI action, bridge, project, or media was contacted or changed. Sentinel authorized this report-only branch after clarifying the initial no-commit instruction.

**Finding:** the video uses Blackmagic's native MCP in Resolve Studio 21.1. Our CLI uses samuelgursky/davinci-resolve-mcp v3.2.0 in granular mode against Resolve 20.3.1 Free through an in-app bridge. We have useful editing primitives, but neither 384 catalog entries nor a successful marker demonstration establishes the video's complete workflow. First expose the resources and higher-level operations already present in our server, then validate a small edit on disposable media. Buying Studio is a decision about reproducing the native workflow, not a prerequisite for every editing outcome.

Evidence baseline: read our skill, CLI, catalog builder and catalog before the transcript. Local server checkout: `c8fbe1887324de9d897e6036efcde60417e33e8c`. Ryan's installed edition/version and earlier timeline/marker verification are Sentinel's supplied live findings, not measurements repeated here. Transcript source: [Creator Magic transcript](C:/Projects/CreatorMagic-V7CtHz8JFWA-transcript.md), [video](https://youtube.com/watch?v=V7CtHz8JFWA). This review did not watch the video frames; visual claims below are the narrator's account, not independently inspected screenshots. The transcript supplies no exact native MCP build, tool schemas, arguments, or complete execution log.

## 1. What the video actually does

| Timestamp | Agent work and reported outcome | Human contribution / limit |
| --- | --- | --- |
| 01:08–04:33 | Discovers the MCP bundled with **Resolve Studio 21.1**, registers it and reports a successful handshake. Host is **Codex desktop on Mac**, using **GPT-6 Astra High** (04:52). Exact Resolve patch/native MCP build is unstated. | Mike installs/opens Resolve, creates a project, imports clips and changes frame rate (02:04–02:19); restarts the MCP connection through Codex settings. |
| 04:34–05:38 | Inspects two approximately 75-minute recordings: program picture at 4K/60 fps and a 5K screen recording. Starts transcription and plans separate tracks with aligned audio. | Requests a rough edit using MCP, explicitly excluding cursor control. Inputs are already imported. |
| 06:07–07:23 | Reports a transcription timeout while Resolve continues; later places two video tracks and program audio. Waveform alignment is **not confirmed**. | Tells the agent transcription finished. AssemblyAI's claimed 20-second transcription is a comparison with his other workflow, not the demonstrated backend. |
| 07:31–11:38 | Reviews images, identifies retakes/waits, keeps the existing program composition, chooses screen cutaways, and builds a new rough-cut timeline. Corrects a reported one-frame trim discrepancy. Reports **21:59, 96 sections, 10 screen cutaways**, linked audio/video and preserved full-length source timeline. Adds chapter, missing-insert and exposed-token warning markers. | About 30 minutes to the rough cut; the published video compresses elapsed time. Missing narration/inserts are flagged, not produced. |
| 11:45–14:17 | Playback reportedly shows acceptable cuts and audio dips. On request, shortens one silence by 1.67 seconds while keeping tracks aligned. | Mike samples playback, identifies the remaining pause at transcript notation `1:20:11`, requests only that fix, then demonstrates Command-Z and a manual Shift-Delete. The time notation's frame/second interpretation is not established. Agent correction took nearly two minutes. |
| 14:43–16:04 | Creates a five-second lower third with name, channel, teal accent and fades, inserts it on an overlay track and reviews an image. | Requests the title text; supplies no house style. Roughly three minutes. No general B-roll generation is demonstrated. |
| 16:18–19:34 | Backs up the approved edit, reports a restrained grade, performs program audio analysis and saves a YouTube 4K render preset. Compaction takes about four minutes; finishing takes about 15 minutes. Python helper files are explicitly observed (17:53–18:05). | Human requests grade/loudness preparation but initially prohibits rendering. He sees little obvious grade difference. Transcript says `14.69 LUFS` and `1.04 decibels true peak` without reliable signs; do not turn this into verified negative readings or an independently verified compliance result. |
| 19:38–20:42 | After permission, queues and starts an MP4/4K render; reports its destination. | Mike explicitly says render it. The excerpt establishes **render started**, not successful completion, decoded-file QC or upload. Approximately one hour elapsed before rendering. |

The outro's existing Claude-based editing process and future automatic publishing are separate from this demonstration. This is useful agent-assisted rough editing and finishing with human review, not demonstrated zero-intervention publishing.

## 2. Boundary map to our exact catalog

Commands below are `resolve <area> <action>` names from [catalog.json](C:/Users/Ryan/.claude/skills/ls-davinci-resolve/catalog.json). An entry proves routing/schema presence, not live success on Ryan's build. NONE means no equivalent complete action in this catalog; adjacent primitives are stated separately.

| Video step | Our catalog equivalent | Boundary |
| --- | --- | --- |
| Connect / inspect project | Launcher and bridge setup; `app get-version-fields`; `item ti-get-info`; `timeline list-timelines-tool` | **NONE** for the complete project/media/timeline discovery resources through this CLI. The granular server exposes resources such as `resolve://current-project`, `resolve://media-pool-clips` and `resolve://timeline-items`, but the CLI only calls tools. |
| Create/import/set rate | `project create`, `project set-setting`, `media-pool import-media` | Present; video human did these. Actual source codec support still matters. |
| Stack and align sources | `timeline create`, `timeline add-track`, `media-pool append-to-timeline`, `media-pool setup-multicam-timeline`, `media-pool auto-sync-audio`, `timeline set-clips-linked` | Positioned append accepts source ranges, record frames, track index and video/audio selection. Stacking is not proof of synchronization; waveform sync is content-dependent. |
| Transcribe and recover timeout | `clip transcribe-audio`, `clip transcribe-clip-audio`, `folder transcribe-audio` | Native transcription is Studio-only on Resolve 20. **NONE** for a complete external timestamped-transcript pipeline, readable transcript retrieval and persistent job recovery in this catalog. |
| Decide cuts / preserve source / assemble 96 sections | `timeline duplicate`, `timeline create`, `media-pool append-to-timeline`, `timeline delete-clips`, `timeline set-clips-linked` | **NONE** for the full editorial decision/validation workflow. Batch positioned append is useful; a frame-accurate keep-range plan and linked-track checks must precede it. |
| Review frames / select cutaways | `timeline set-current-timecode`, `timeline get-current-clip-thumbnail`, `project export-current-frame-as-still`; cutaways via positioned append | Tools exist for obtaining frames, but image interpretation belongs to the model. CLI drops non-text MCP payloads; path-based still inspection needs a host-side image step. |
| Chapters / missing inserts / token warnings | `timeline add-marker`, `timeline get-markers`, `clip add-marker` | Marker writes exist; semantic detection and deciding what to flag remain agent/workflow work. |
| One targeted silence trim | `timeline duplicate`, `timeline delete-clips`, `media-pool append-to-timeline` | **NONE** for an arbitrary-range, all-track, verified trim as one catalog operation. Ripple deletion of whole items is not automatically the demonstrated precise trim. |
| Audio dips / smooth cut edges | `item set-audio`, `item add-keyframe`; `resolve set-timeline-item-fades` | Gain/keyframe wrappers do not prove envelope support. Native fade setter is **21.1+**, absent on 20.3.1. Need a validated older-build route or externally prepared audio. |
| Custom lower third | `timeline insert-title`, `timeline insert-fusion-title`, `timeline insert-fusion-composition`, `item ti-import-fusion-comp`, `item set-composite` | **NONE** for full custom Text+/Fusion node construction and text/animation editing in this catalog. Importing an authored `.comp` or overlay asset is a possible alternative. |
| Grade | `item ti-set-cdl`, `item ti-copy-grades`, `graph set-lut`, `graph apply-grade-from-drx` | Basic grade operations are present. Judging skin tones and preserving screen colors needs before/after rendered evidence. No demonstrated requirement for Studio-only effects. |
| Measure / correct program loudness | `item set-audio`, `project apply-fairlight-preset-to-current-timeline`; `resolve normalize-timeline-audio-level` | **NONE** for full-program LUFS/true-peak measurement in our catalog. Native normalize wrapper requires 21.1. Per-clip gain or a preset is not final-program compliance. |
| Configure / render / verify | `project set-current-render-format-and-codec`, `project set-render-settings`, `project save-as-new-render-preset`, `project add-render-job`, `project start-rendering-jobs`, `project get-render-job-status` | Render controls exist. **NONE** for a finished-file decode/loudness/visual-QC workflow in the catalog. Video itself stops before that proof. |

Our [resolve.py](C:/Users/Ryan/.claude/skills/ls-davinci-resolve/resolve.py:55) starts `src/server.py --full` for **every action**, opens an MCP stdio session and closes it afterward. [build_catalog.py](C:/Users/Ryan/.claude/skills/ls-davinci-resolve/build_catalog.py:96) freezes tools only, routing by granular source module. Thus this CLI still uses MCP; it is a shell client rather than Codex's persistent, directly registered tool connection. It has no resource/prompt route and preserves only the type of non-text content. The skill estimates approximately two seconds startup per call; not benchmarked here.

Crucially, `--full` selects a **different server instance**, not the compound instance plus extra tools: [server.py](E:/SAS/REPO_CLONES/davinci-resolve-mcp/src/server.py:32676). The default instance already contains `media_analysis`, `edit_engine`, richer `timeline` audio/edit operations, and `fusion_comp`. For example, [edit_engine](E:/SAS/REPO_CLONES/davinci-resolve-mcp/src/server.py:24001) plans and assembles tightened variants with mirrored audio. These are upstream code we have, but our 384-action route does not expose them. The separate advanced/offline surface has further delivery/audio facilities; it is also outside this CLI. Neither surface is Blackmagic's native MCP, and the transcript cannot supply a native-tool-by-native-tool comparison.

## 3. Windows versus macOS

The video explicitly states Mac at 03:55–04:00 and Mac Studio/ProRes recording at 23:05–23:13. It does not show us a reproducible native MCP command, install path or transport configuration. Do not translate an imagined macOS command into a Windows installer recipe.

Our concrete Windows route is [resolve.cmd](C:/Users/Ryan/.local/bin/resolve.cmd), the clone's `venv/Scripts/python.exe`, and a per-Resolve-launch in-app bridge on loopback port **49632**. Script locations are the `%PROGRAMDATA%` and `%APPDATA%` Blackmagic Design/DaVinci Resolve/Support/Fusion/Scripts/Utility trees. This bridge difference is primarily **edition/version**, not a requirement intrinsic to Windows. Mac `launchctl`/Homebrew instructions do not apply to this box. Command-Z becomes Ctrl-Z; keyboard equivalents are human fallbacks, not required MCP operations.

No step in the transcript establishes a macOS-only Fusion or Fairlight algorithm. Fonts, asset paths, codecs and Python discovery need Windows validation. A 5K input does not itself demand a 5K deliverable: keep the proposed free-edition output within supported UHD/60 limits. Exact source codec/bit-depth support is unverified. `ffmpeg.exe` and `ffprobe.exe` resolve on this machine's PATH; no media analysis was run. Bridge media/output roots are constrained: inputs outside the configured user tree and exports outside its Movies tree require deliberate configuration, not assumed access. Authentication values are omitted.

## 4. Version and edition boundaries

- **20.3.1 Free:** retain the established bridge route. Native Studio transcription is unavailable; use a separately authorized local/external transcript pipeline for equivalent editorial input. Blackmagic explicitly identifies Resolve 20 AI transcription as Studio-only in its [Editors Guide](https://documents.blackmagicdesign.com/UserManuals/DaVinci-Resolve-20-Editors-Guide.pdf?_v=1757574010000). Ordinary timeline assembly, markers, basic titles/color and supported exports are not inherently Studio-only, but our exact RPC paths need validation.
- **21.1 Free:** upgrading breaks the established Python bridge launch route; it does not unlock the video's Studio workflow. This follows the supplied deployment finding and the upstream maintainer's [bridge compatibility notes](E:/SAS/REPO_CLONES/davinci-resolve-mcp/docs/SKILL.md:22). Do not infer that every possible Lua/internal route is impossible; none is an established replacement here.
- **21.1 Studio:** matches the video's stated edition/version, including its native MCP and transcription route. Native `SetFades`/`SetSpeed` and normalization wrappers in our catalog have a 21.1 floor: [fade documentation](E:/SAS/REPO_CLONES/davinci-resolve-mcp/docs/reference/resolve211-speed-fades.md), [normalization documentation](E:/SAS/REPO_CLONES/davinci-resolve-mcp/docs/reference/resolve211-normalization.md). A listed action does not backport its API to 20.3.1.
- **Not proved Studio requirements:** basic lower thirds, ordinary grade changes, program loudness measurement outside Resolve, and supported UHD MP4 rendering. The video does not demonstrate Magic Mask, neural noise reduction, voice isolation or other effects that would justify classifying its whole finishing pass as Studio-only. Codec/output limits are separate from scripting access; see [Blackmagic's edition comparison](https://www.blackmagicdesign.com/products/davinciresolve/studio).
- **Not solved merely by buying Studio:** full Fairlight automation graphs and arbitrary parameter access are not universally available through this third-party API. Its [audio boundary report](E:/SAS/REPO_CLONES/davinci-resolve-mcp/docs/kernels/audio-fairlight-kernel.md:88) documents rejected property writes and incomplete automation support. The video never identifies the specific mechanism used for its dips.

## 5. Concrete gaps and effort

Estimates classify bounded work, not elapsed-time promises. Larger workflows may require several cards.

1. **Resources, prompts and image-preserving responses — a card.** Add discovery/read routes and retain native images or explicit image artifacts. This closes a real client limitation independently of edition. Fix skill examples against the frozen schema: e.g. its `project get-current-project-name` example is not in this catalog, and its `item set-transform --pan` example does not match the catalog's property-name/property-value arguments.
2. **Compound/advanced route selection — a card.** Expose selected existing `media_analysis`, `edit_engine`, audio and Fusion operations with explicit server provenance. Rebuilding the current `--full` catalog alone cannot expose the other server instance. A new third-party checkout is unnecessary for these operations.
3. **Timestamped transcription and long-job recovery — a card; exact native Studio transcription is not possible on Free.** Choose a local backend or authorized service, persist timestamps/status and recover timeouts without duplicate work. Existing backend installation/model readiness was not audited.
4. **Editorial recipe and linked-track acceptance — a card.** Define retake/silence rules, source preservation, timebase/end-frame semantics, screen cutaways and resume checkpoints; verify complete thoughts and sync with human playback. The 384 wrappers contain no automatic editorial taste.
5. **Older-build audio fades and program QC — a card.** Prove a 20.3.1 route, potentially prepared audio plus import; measure the actual final program with FFmpeg. Do not promise `add-keyframe` gives working audio envelopes or native 21.1 normalize exists on this build.
6. **Lower-third template and rendered verification — a card.** Provide a Windows-compatible font/style/template and a tested Fusion import or media-overlay path. Full custom authoring is not a single current catalog action.
7. **Reduced per-call overhead and resumable operation state — a card.** Prefer batching already supported clip-info arrays first; consider a resident stdio client after measurement. Per-call server startup also requires checking which upstream state persists across invocations.
8. **Per-launch bridge / authorized media paths / correct examples — trivial preparation.** Setup is already installed, so do not count it as missing software. At the next approved scratch run, the human starts the bridge and the agent checks readiness; current task deliberately made no such call.
9. **Complete workflow acceptance — a card.** Earlier timeline/marker evidence covers only those actions. Need a synthetic, disposable test from import through synced cuts, title, fades, approved render and decoded-output QC. Exact native 21.1 MCP reproduction on 20.3.1 Free is not possible; reproducing most outcomes remains a separate, testable target.

## 6. Recommended next steps

1. **Cards, no purchase required:** correct examples; add resource/image support; expose a small selected compound surface from the existing clone. Keep changes separate from the pending Codex skill port owned elsewhere.
2. **Ryan decision:** choose staying on 20.3.1 Free with external transcription versus Studio/native-MCP parity. Preserve the intentional no-upgrade decision until he changes it. Also choose local transcription versus a paid/cloud service before installing/downloading or uploading media. Do not install a second server just because the video says MCP.
3. **Cards:** build the timestamped editorial plan and resumable execution flow, then the title/audio paths. Start with a one-minute synthetic fixture using two aligned video tracks and linked audio; test frame-boundary and timeout recovery explicitly.
4. **Ryan authorization for a live scratch run:** create only a disposable project/duplicate and run the acceptance sequence. The live project on monitor 1 remains outside this report's scope. Inspect rendered frames, listen to cut boundaries, check measured loudness and completion; successful API returns alone do not qualify.
5. **If Ryan chooses Studio:** inspect the native MCP shipped with the installed Windows build and compare its real tool schemas with the selected third-party operations. Only then decide whether native MCP replaces or supplements our client. The transcript supplies too little protocol evidence to decide that today.

Validation boundary: catalog/schema and source reads, transcript review, Windows executable-path discovery, and primary documentation lookup only. The catalog contains 384 routed tools and zero unrouted tools. No live Resolve validation, installs, model loading, media processing or project mutations. The required repository lint was run (`uv run --locked --no-sync ruff check . --no-cache --statistics`): 750 existing Python findings on the unchanged source baseline; this is not a clean lint result. Source changes are outside T708; only this report is delivered. Report links and Git whitespace checks were checked separately.
